{
  "description": "model-router: session.start, prompt.submit, command.run{command=route}, turn.step. Needs CLAUDE_CODE_ENABLE_FUNCTION_HOOKS=1; builds without function hooks ignore the modules key.",
  "modules": ["./register.ts"]
}
